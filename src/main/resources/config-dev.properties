# for configurations specific to "development" environment
# this will override default values in config.properties

# ====== how to use? ========
# just need to set the active profile at runtime
# java -Dactive.profile=dev -jar application.jar
